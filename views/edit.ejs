<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logged in</title>
    <script src="https://cdn.tailwindcss.com" ></script>
</head>
<body>
    <div class="w-full h-screen p-10 text-white bg-zinc-900">
        <div class="flex w-full justify-end">
            <a class="bg-red-600  rounded-md px-3 py-2 text-sm mb-5 inline-block" href="/logout"> Logout</a>
        </div>

        <h3 class="text-3xl text-zinc-300 mb-3"><span class="font-light">Edit Your Post</span>,✌🏻</h3>

        <form method="post" action="/update/<%= post._id %>">
            <textarea name="content" placeholder="What's on your mind" class=" p-3 outline-none w-1/3  resize-none bg-transparent border-2 border-zinc-800 rounded-lg block"><%= post.content %></textarea>

            <input class="px-3 text-sm w-1/6 py-2 mt-3 text-black bg-yellow-500 rounded-md block" value="Update post" type="submit">
        </form>

        
    </div>
</body>
</html>